#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
    vector<vector<int> > generateMatrix(int n) {
        int direction = 0;
        vector<vector<int> > matrix(n, vector<int>(n, 0));
        int num = 1, count = n * n;

        int ix = 0;
        int row = ix, column = ix;
    }
};

int main() {
    Solution s;

    return 0;
}
