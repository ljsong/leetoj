#include <vector>
#include <iostream>
using namespace std;

class Solution {
public:
    vector<int> twoSum(vector<int>& numbers, int target) {
        int low = 0, high = numbers.size();
    }

private:
    void findTarget(vector<int>& numbers, int low, int high, int target) {

    }
};

int main() {
    Solution s;
    vector<int> nums = {2, 7, 11, 15};
    s.twoSum(nums, 9);

    return 0;
}
